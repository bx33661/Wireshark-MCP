# 功能迭代路线图

[English](ROADMAP.md)

这份路线图约束版本结果，不承诺具体日期。代码、测试、用户文档和发布记录都齐全后，一项功能才算完成。

## 状态说明

- **进行中**：当前分支已经实现，或正在做发布验收。
- **下一版**：范围已经收敛，优先进入下一个小版本。
- **后续**：方向确定，排在下一版之后。
- **候选**：有价值，但还缺接口方案或真实需求证据。

## 3.0 正式版：可信分析与稳定接口

- [x] 稳定结果契约：字符超限时不破坏数据类型（保持 dict/list），支持结构化分页，MCP 层同步 `CallToolResult.isError`。
- [x] 校准检测语义：引入最小 Finding 发现模型，修复短凭据漏报（`<20` 字符），DoS 增加时间窗与速率语义，DNS 严格过滤查询。
- [x] 确定性 PCAP 样本与真实回归测试（`test_real_pcap.py` 与 CI 全平台集成）。
- [x] 资源安全边界：流式循环补充总字节上限熔断，聚合引入分组数量与去重基数上限。
- [x] 协议与传输矩阵：真实 stdio 进程间通信握手验证，2026-07-28 协议检验，编写跨平台与客户端兼容矩阵。
- [x] 假设驱动的调查 Skill：重构 playbooks，确立必须包含可复现包级证据的报告标准，完成 `tshark-cli-analysis` 最小原型。
- [x] 支持全量计数、分组、去重基数、Top-K 和时间分桶。
- [x] 聚合扫描超过一百万个匹配包时提前终止。
- [x] 文件写入工具运行前必须配置可写根目录。
- [x] 默认拒绝未经认证、监听非回环地址的 HTTP；管理员必须显式确认。

完成标准：分析结果可复现，扫描不完整时明确说明，重要结论具备确凿的数据包复现路径。

## 下一版：3.1 深度分析、内存边界与 CLI Skill 正式版

- [x] **Agent 流量分析评测基线**：用固定问题、确定性抓包、预期事实、证据要求和禁止性结论评估 Skill 对正确性、结论校准与调用成本的影响。
- [x] **真·流式聚合管道**：改为逐行消费的累加器（accumulator），彻底消除大文件聚合时的全量文本中间件与内存暴涨。
- [x] **数值聚合**：为数值型 tshark 字段增加 `sum`、`min`、`max`、`avg`，覆盖字节数、长度和响应时间。
- [ ] **独立 tshark CLI Skill 正式版**：完善终端自动化、管道过滤和脚本批处理指南。
- [ ] **MCP vs 原生 CLI 对照基准**：可复现执行基准和准确率门控的 Agent 对比工具已经完成；仍需发布独立多轮 Agent 结果和大抓包伸缩数据。
- [ ] **可配置扫描策略**：在安全的服务端边界内，支持配置聚合包数上限和超时。

完成标准：数值聚合和分页有真实 tshark 测试，输出稳定、内存有界，中英文聚合指南都有示例。

## 后续：3.2 证据优先的发现模型

- 让安全与异常工具共用一套发现结构：严重性、置信度、证据帧、过滤器、流索引和限制条件。
- Markdown 与 JSON 报告从同一份发现数据生成，不再维护两套互不相干的输出逻辑。
- 增加时间线辅助能力，为事件响应保留准确帧号和相对时间。
- 支持比较两个抓包或两个过滤时间窗，同时避免把包体大规模送入模型上下文。

完成标准：每条自动发现都能回溯到明确的工具输出，并给出包级核验路径。

## 后续：3.3 运行安全加固

- 给出 Streamable HTTP 经可信反向代理部署时的认证方案与自动检查。
- 建立受支持 Wireshark/tshark 版本矩阵的兼容性测试。
- 发布机器可读的能力与兼容性文档，供客户端和打包系统读取。

完成标准：远程认证部署可以稳定复现；平台与工具链支持矩阵有 CI 结果或人工验收记录。

## 候选项

- 带来源和更新时间的本地指纹、IOC 数据源插件。
- 面向大型离线任务的可选列式导出。
- 保存过滤器、帧号、流索引和报告元数据的调查清单。
- 在字段稳定且有人维护测试抓包时增加新的协议包。

## 暂不考虑

- 替代 Wireshark GUI 或协议解析引擎。
- 默认把抓包上传到托管服务。
- 在没有数据包证据时判定流量恶意。
- 已有参数化工具能够表达的能力，不再额外增加同义工具。

提交功能请求时，请说明要回答的实际问题、期望证据、相关 tshark 命令或字段；抓包可以安全分享时，再附最小样本。
